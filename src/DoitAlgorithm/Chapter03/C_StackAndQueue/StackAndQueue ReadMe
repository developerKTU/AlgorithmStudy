※ 스택과 큐

- 배열에서 조금 발전된 자료구조

1. 스택
    - 후입선출 구조 (LIFO)
    - top   : 삽입과 삭제가 일어나는 위치
    - push  : top 위치에서 새로운 데이터 삽입하는 연산
    - pop   : top 위치에서 데이터 빼내고 가져오는 연산
    - peak  : top 위치에 있는 현재 데이터값을 확인하는 연산
    - 스택은 깊이우선 탐색, 백트래킹 알고리즘에서 쓰인다. -> 스택은 재귀함수 알고리즘과 일맥상통한다.

2. 큐
    - 선입선출 구조 (FIFO)
    - rear  : 큐에서 가장 끝 데이터를 가리키는 영역
    - front : 큐에서 가장 첫 데이터를 가리키는 영역
    - add   : rear 부분에 새로운 데이터를 삽입하는 연산
    - poll  : front 부분에 있는 데이터를 빼내고 확인하는 연산
    - peak  : front 부분에 있는 데이터를 확인하는 연산
    - 큐는 너비우선 탐색 알고리즘에서 쓰인다.

3. 우선순위 큐
    - 값이 들어간 순서와 상관없이 우선순위가 높은 데이터가 먼저 나오는 자료구조.
    - 큐 설정에 따라 front에 항상 최댓값 또는 최솟값이 위치함.
    - 우선순위 큐는 일반적으로 힙(Heap)을 이용하여 구현함.
        -> 힙(Heap)은 트리 구조 중 하나.